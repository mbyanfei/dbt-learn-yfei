version: 2

sources:
  - name: learn_stripe
    database: raw
    
    tables:
      - name: charges
        # columns:
        #   - name: id
        #     tests:
        #       - not_null
        #       - unique
          # - name: order_id
          #   description: Foreign key to jaffle_shop orders
          # - name: amount
          #   description: Amount in US cents
      
      - name: refunds
        # columns:
        #   - name: id
        #     tests:
        #       - not_null
        #       - unique
        #   - name: order_id
        #     description: Foreign key to jaffle_shop orders
        #   - name: amount
        #     description: Amount in US cents

  - name: ticket_tailor
    database: raw
    
    tables:
      - name: orders